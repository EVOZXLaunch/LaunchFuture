{
  "name": "launchfuture",
  "version": "1.0.0",
  "private": true,
  "description": "LaunchFuture - Multi-Chain Token Launch Platform",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "npx http-server . -p 8080 -c-1"
  },
  "dependencies": {
    "ethers": "6.13.5"
  },
  "devDependencies": {
    "esbuild": "0.24.2"
  }
}
